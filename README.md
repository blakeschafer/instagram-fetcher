# Instagram Fetcher

Downloads all images, videos, captions, and metadata from an Instagram profile.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
python fetch_instagram.py <username_or_url>
```
